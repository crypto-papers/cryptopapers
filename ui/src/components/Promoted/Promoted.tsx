import React from 'react';

import style from './Promoted.module.scss';

const Promoted: React.VFC = () => <small className={style.promoted}>Promoted</small>;

Promoted.displayName = 'Promoted';

export default Promoted;
