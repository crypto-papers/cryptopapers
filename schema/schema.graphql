interface Node {
  id: ID!
}

type Query {
  currencies: [Currency!]!
  papers: [Paper!]!
  paper: Paper!

  node(id: ID!): Node
}

type Currency {
  id: ID!
  name: String!
  ticker: String!
  # createAt: Time!
}

type Paper {
  id: ID!
  # author: [Author]
  # currency: [Currency]
  description: String
  # excerpt: String
  # file: [File]
  # pageNum: Int
  title: String!
  # createAt: Time
}
